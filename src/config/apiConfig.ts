const BASE_URL = 'https://dummyjson.com';

export const HEADERS = {
  Accept: 'application/json',
  'Content-Type': 'application/json',
};

export const COLLECTIONS = {product: '/products', user: '/users'};

export default BASE_URL;
